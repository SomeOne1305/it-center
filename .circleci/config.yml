version:2.1

jobs:
  build:
    docker:
      - image: node:16
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm Install
      - run:
          name: Build App
          command: npm run build

workflows:
  version: 2
  build:
    jobs:
      - build